id: pulse.go.security.sql-injection-concat
language: go
severity: error
message: |
  SQL injection via string concatenation in database query.
  User-controlled data concatenated into a SQL query string allows arbitrary SQL execution.
  Use parameterized queries instead: db.Query("SELECT * FROM users WHERE id = $1", id)
rule:
  pattern: $DB.Query($QUERY)
  has:
    kind: binary_expression
    stopBy: end
metadata:
  cwe: ["CWE-89"]
  owasp: ["A03:2021"]
  pulse.confidence: HIGH
  pulse.impact: HIGH
  pulse.fix_complexity: LOW
  pulse.reachability_relevant: true
test:
  positive:
    - |
      db.Query("SELECT * FROM users WHERE id = " + userId)
    - |
      db.Query("SELECT * FROM users WHERE name = '" + name + "'")
  negative:
    - |
      db.Query("SELECT * FROM users WHERE id = $1", userId)
    - |
      db.Query("SELECT * FROM users")
